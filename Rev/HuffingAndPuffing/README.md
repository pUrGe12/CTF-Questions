To compile the binary for a CTF challenge, 

    g++ -d --static -o name.exe question.cpp

The additional flags are to make sure that the binary can be analysed through a decompiling software like Ghidra or IDA. 
